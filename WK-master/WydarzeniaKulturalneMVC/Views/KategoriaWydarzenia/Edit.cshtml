@model WydarzeniaKulturalne.Data.Entities.KategoriaWydarzenia

@{
    ViewData["Title"] = "Kategoria";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<div class="card shadow m-4">
    <h4 class="ml-3 mb-3 py-3"> Kategoria Wydarzenia</h4>
    <div class="row">
        <div class="col-lg-8 offset-lg-2">
            <form enctype="multipart/form-data" asp-action="Edit">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <input type="hidden" asp-for="id" />

                <div class="form-group">
                    <a asp-action="Index" class="btn btn-warning">Powrót</a>
                    <input type="submit" value="Zapisz" class="btn btn-primary" />
                </div>
                <div class="form-group">
                    <label asp-for="Nazwa" class="control-label"></label>
                    <input asp-for="Nazwa" class="form-control" />
                    <span asp-validation-for="Nazwa" class="text-danger"></span>
                </div>

                <div class="form-group">
                    <label asp-for="Opis" class="control-label"></label>

                    <textarea asp-for="Opis" id="content"></textarea>
                    <span asp-validation-for="Opis" class="text-danger"></span>
                </div>

            </form>
        </div>
    </div>
</div>
@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }

    @*    <script>

            document.addEventListener('DOMContentLoaded',() => {
                        var input = document.getElementsByClassName("custom-file-input")[0];
                        var label = document.getElementsByClassName("custom-file-label")[0];
                        input.onchange = (event) => {
                            label.innerHTML = event.target.files[0].name;
                        }

            });
    </script>*@
    <script>
        tinymce.init({
            forced_root_block: "",
            selector: 'textarea',
            menubar: false,
            statusbar: false,
            language: 'pl'
        });
    </script>

}



