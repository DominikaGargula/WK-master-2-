@model IEnumerable<WydarzeniaKulturalne.Data.Entities.WydarzenieKulturalne>

@{
    ViewData["Title"] = "Index";
}



<section class="py-5">
    <div class="container px-4 px-lg-5 mt-5">
        <h1>Index</h1>
        <p>  <a asp-action="Create">Create New</a></p>
        <div class="row gx-4 gx-lg-5 row-cols-2 row-cols-md-3 row-cols-xl-4 justify-content-center">
            <p>
       
            </p>
            <table class="table align-middle mb-0 bg-white">
                <thead class="bg-light">
                    <tr>
                        <th>
                            @Html.DisplayNameFor(model => model.Nazwa)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.Opis)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.ZdjecieUrl)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.Cena)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.DataUwtorzenia)
                        </th>
                        <th>
                   
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.KategoriaWydarzenia)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.Promowane)
                        </th>

                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.Nazwa)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Opis)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.ZdjecieUrl)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Cena)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.DataUwtorzenia)
                            </td>
                            <td>
                           
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.KategoriaWydarzenia.Nazwa)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Promowane)
                            </td>

                            <td>
                                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                                <a asp-action="DetailsCard" asp-route-id="@item.Id">DCard</a>
                                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>

    </section>